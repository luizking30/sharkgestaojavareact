# Dev local: vazio → api.js usa http://<hostname>:8080
# Produção: use https://api.sharkgestao.com ou deixe vazio — o build lê .env.production
# (VITE_API_URL=https://api.sharkgestao.com). Na Vercel você pode definir o mesmo em Environment Variables.
VITE_API_URL=
